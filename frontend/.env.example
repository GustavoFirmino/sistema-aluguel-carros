# URL base da API (backend).
# Em desenvolvimento o proxy do Vite já redireciona automaticamente,
# então esta variável só é necessária em builds de produção.
# Copie este arquivo para .env.local e ajuste conforme o ambiente.
VITE_API_URL=http://localhost:8080
