# Variáveis de ambiente para o docker-compose (produção).
# Copie este arquivo para .env e ajuste os valores antes de rodar:
#
#   cp .env.example .env
#   docker compose up --build

# Credenciais do PostgreSQL
DB_USER=postgres
DB_PASSWORD=postgres
